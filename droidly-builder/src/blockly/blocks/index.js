import './layout'

const layoutCategoryContent = {
  kind: 'category',
  name: 'Layout',
  colour: '#0be881',
  contents: [
    {
      kind: 'block',
      type: 'layout_row'
    }
  ]
}

const contents = [
  layoutCategoryContent,
]

export default contents
